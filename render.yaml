services:
  - type: web
    name: sandoogh14-bot
    runtime: python
    plan: free
    envVars:
      - key: TELEGRAM_TOKEN
        sync: false
    buildCommand: pip install -r requirements.txt
    startCommand: python telegram_bot_render.py